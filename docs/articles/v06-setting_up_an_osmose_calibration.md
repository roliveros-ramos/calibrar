# Setting up an OSMOSE calibration on high-performance computing (HPC) systems using the calibrar package

Vignettes are long form documentation commonly included in packages.
Because they are part of the distribution of the package, they need to
be as compact as possible. The `html_vignette` output type provides a
custom style sheet (and tweaks some options) to ensure that the
resulting html is as small as possible. The `html_vignette` format:

- Never uses retina figures
- Has a smaller default figure size
- Uses a custom CSS stylesheet instead of the default Twitter Bootstrap
  style

## Using the OSMOSE observation module

### Surveys

### Post-processing OSMOSE outputs

## Using the `osmose_calibration_setup()` function

## Filling the data templates

## The `osmose_calibration_setup()` again, with data

## Editing the `calibration_settings.csv` file

## Editing the parameter configuration files

### The parameter guess file (`parguess.csv`)

### The parameter bounds files (`parmin.csv` and `parmax.csv`)

### The parameter phase file (`parphase.csv`)

## Running the `osmose_calibration_test()`

## Launching a calibration in a HPC system

## Optimisation options (`.calibrarrc`)

## Monitoring the progress with `osmose_calibration_check()`
