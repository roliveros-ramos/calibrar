

print.calibrar = function(x, ...) {
  print(list(x$par, x$value, x$counts), ...)
}

coef.calibrar = function(x, ...) {
  
}

plot.calibrar = function(x, ...) {
  
}

summary.calibrar = function(x, ...) {
  
}

print.summary.calibrar = function(x, ...) {
  
}


